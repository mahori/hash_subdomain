#ifndef UTILS_H
#define UTILS_H

extern std::string get_alphabets(std::size_t hash_size, std::size_t hash_value);
extern std::size_t number_of_alphabets(void);
extern std::size_t hash_size(std::size_t length);

#endif  // UTILS_H 
